/**
 * using array method, you should return
 * an array with phones whose price is higher than 800 ( Ex: [{ name: "Samsung Galaxy Note 20", price: 900 }, ...] )
 * an array with tablets whose price is lower than 600
 * an array with phones whose names don't include "Samsung"
 * an array with tablets whose name don't include "Pro"
 **/
